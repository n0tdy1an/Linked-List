class Node:
    def __init__(self, data):
        self.data = data  
        self.next = None 
class LinkedList:
    def __init__(self):
        self.head = None
           
    def insertAtBegin(self, data):
        newNode = Node(data)  
        newNode.next = self.head  
        self.head = newNode 

    def insertAtIndex(self, data, index):
        if index == 0:
            self.insertAtBegin(data)  
            return
       
        newNode = Node(data) 
        currentNode = self.head  
        position = 0
       
       
        while currentNode is not None and position < index - 1:
            position += 1
            currentNode = currentNode.next
       
        if currentNode is None:
            print("Index not present.")  
            return
       
        newNode.next = currentNode.next  
        currentNode.next = newNode  

    def insertAtEnd(self, data):
        newNode = Node(data)  
        if self.head is None:  
            self.head = newNode
            return
       
        currentNode = self.head 
        while currentNode.next: 
            currentNode = currentNode.next
       
        currentNode.next = newNode 

    def updateNode(self, val, index):
        currentNode = self.head  
        position = 0
       
       
        while currentNode is not None and position != index:
            position += 1
            currentNode = currentNode.next
       
        if currentNode is None:
            print("Index not present.")  
        else:
            currentNode.data = val  

    def removeFirstNode(self):
        if self.head is None:  
            return
        self.head = self.head.next 
       
    def printList(self):
        currentNode = self.head  
        while currentNode:
            print(currentNode.data, end=" -> ") 
            currentNode = currentNode.next  
        print("None") 
JuniorList = LinkedList()

JuniorList.insertAtBegin("Martins")
JuniorList.insertAtIndex("Brandon", 1)
JuniorList.insertAtIndex("Chris", 2)
JuniorList.insertAtIndex("Alex", 3)
JuniorList.insertAtIndex("Abraham", 4)
JuniorList.insertAtIndex("Tyler", 5)
JuniorList.insertAtIndex("Andrew", 6)
JuniorList.insertAtIndex("Wes", 7)
JuniorList.insertAtIndex("Caleb", 8)
JuniorList.insertAtEnd("Dylan")

JuniorList.printList()
